[project]
name = "embed-fixer"
version = "0.1.0"
description = "fixing visual embeds for links in discord"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "py-cord>=2.6.1",
    "google-generativeai>=0.8.0",
    "python-dotenv>=1.0.0",
]